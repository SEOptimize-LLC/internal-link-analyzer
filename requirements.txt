# Streamlit Cloud Optimized Requirements
# Minimal dependencies to avoid conflicts and deployment issues

streamlit==1.28.1
requests==2.31.0
beautifulsoup4==4.12.2
pandas==2.1.4
lxml==4.9.4

# Note: Using exact versions known to work well with Streamlit Cloud
# Heavy packages removed to prevent deployment failures:
# - nltk (requires data downloads)
# - openpyxl (can cause conflicts)
# - reportlab (resource intensive)
# - defusedxml (not essential)